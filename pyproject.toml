[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ownlock"
version = "0.1.0"
description = "Lightweight secrets manager — encrypted vault, env injection, stdout redaction. No Docker, no server, no account."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Ownly" }]
keywords = ["secrets", "vault", "env", "encryption", "cli"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
    "Topic :: Software Development :: Build Tools",
]
dependencies = [
    "typer>=0.15.0",
    "rich>=13.0",
    "cryptography>=44.0",
    "keyring>=25.0",
    "python-dotenv>=1.0",
]

[project.scripts]
ownlock = "ownlock.cli:app"

[project.urls]
Repository = "https://github.com/thebscolaro/ownlock"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
